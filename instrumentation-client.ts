/**
 * Client-side instrumentation
 *
 * BotID SDK is disabled - requires additional Vercel configuration
 * Using Vercel's built-in Bot Management + HCaptcha for protection instead
 */

export async function register() {
  // No client-side instrumentation needed
  // Using Vercel Bot Management + HCaptcha instead of BotID
}
